<?php

$doc = new DOMDocument();
$doc->formatOutput = true;

$rootElement = $doc->createElement("response");
$doc->appendChild($rootElement);


foreach ($this->getVars() as $key => $value):
	Data_Xml::toXml($doc, $rootElement, $key, $value);
endforeach;

print $doc->saveXML();